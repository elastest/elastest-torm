package io.elastest.etm.dao;

import org.springframework.data.jpa.repository.JpaRepository;

import io.elastest.etm.model.EimMonitoringConfig;

public interface EimMonitoringConfigRepository extends JpaRepository<EimMonitoringConfig, Long> {

}
