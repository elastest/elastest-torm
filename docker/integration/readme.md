```
docker-compose -f edm-docker-compose.yml -f docker-compose-dev.yml -f esm-docker-compose.yml -f eim-docker-compose.yml up -d
```

```
docker-compose -f edm-docker-compose.yml -f docker-compose-dev.yml -f esm-docker-compose.yml -f eim-docker-compose.yml down
```
