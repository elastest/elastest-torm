version: '3'
services:
    etm-proxy:
        image: elastest/etm-proxy
        expose:
            - 80
        ports:
            - 80:80
        depends_on:
            - etm
        networks:
            - elastest
networks:
    elastest:
      driver: bridge
